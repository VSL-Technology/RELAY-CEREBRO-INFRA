############################################
# Obrigatórios em produção (relay <-> backend)
############################################
RELAY_INTERNAL_TOKEN=        # rotas ops/admin
RELAY_TOKEN_TOOLS=           # rotas leves (ping/arp/health)
RELAY_TOKEN_EXEC=            # rotas de execução (exec-by-device)
RELAY_API_SECRET=            # HMAC compartilhado com backend

############################################
# Runtime / bind
############################################
RELAY_DRY_RUN=0
PORT=3001
BIND_HOST=127.0.0.1

############################################
# Mikrotik (obrigatório fora do DRY_RUN)
############################################
MIKROTIK_NODES=__JSON_ARRAY__

############################################
# WireGuard (obrigatório fora do DRY_RUN)
############################################
WG_INTERFACE=wg0
